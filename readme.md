# Trabalho Análise de dados

## Como rodar
Crie um venv python e instale as dependencias

```
python3 -m venv .venv
source .venv/bin/activate
pip install -r ./requirements.txt
```

Selecione o .venv como kernel do notebook execute-o